# ord는 문자열을 아스키 코드로 바꿔주는 함수입니다
# chr는 아스키 코드를 문자열로 변환해주는 함수입니다
# strip은 문장에 있는 양쪽 공백을 제거하는 함수입니다. (lstrip 은 왼쪽, rstrip은 오른쪽 공백 제거)
# replace 문자나 문자열을 대체하거나 제거할 수 있음
# eval() 실행하고자 하는 표현식을 문자열로 전달하면 표현식의 결과값을 반환



# for i in range(10, -1 , -1): 
#     print(i) # 10부터 0까지 -1씩 출력

# for i in range(9):
#     b = int(input())
#     a.append(b)

# birthday = p.split("-")[0]
# print("생년월일은 {}입니다.".format(birthday))
# break

# 리스트 안에 딕셔너리를 넣는 이유는 student = [{"name" : 80}] 이렇게면 student["name"] 으로 들고오면 됨 

# a = [1, 2, 3]
# a.index(1) 의 답은 0

# if image_url.startswith("//"): # 만약 image_url 이게 "//" 으로 시작한다면: